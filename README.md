# wstunnel на Render

Этот проект разворачивает только **wstunnel-сервер** как Render Web Service. Render принимает внешний HTTPS/WSS-трафик и передаёт WebSocket в контейнер. Сервер wstunnel не является WireGuard-сервером и не создаёт полноценный VPN-интерфейс сам по себе: клиентское устройство получает локальный SOCKS5-прокси, через который можно направлять трафик.

## Что получится

Схема выглядит так:

```text
Телефон: wstunnel client
    локальный SOCKS5 127.0.0.1:10808
                │
                │ WSS через HTTPS
                ▼
Render Web Service: wstunnel server
                │
                ▼
        сайты и TCP-сервисы в интернете
```

Для просмотра сайтов в одном приложении можно указать SOCKS5-прокси `127.0.0.1:10808`. Для маршрутизации всего Android-трафика проще всего использовать **SocksDroid**: он принимает SOCKS5 и создаёт системный Android VPN через `VpnService` без root. Это отдельный компонент, не встроенный в wstunnel.

## Деплой в Render

Создай новый Web Service из этого репозитория. Render должен использовать Dockerfile из корня проекта. В настройках сервиса установи переменную окружения:

```text
WSTUNNEL_PATH=<длинная случайная строка>
```

Пример безопасного значения:

```text
WSTUNNEL_PATH=7f3c9a2e1d5b4c8a6e0f9b2d4a7c1e8
```

Не публикуй это значение в GitHub, README, скриншотах или сообщениях. Оно выполняет роль общего секрета: сервер принимает только WebSocket upgrade-запросы с этим path prefix.

В `render.yaml` выбран бесплатный инстанс, чтобы начать без платного включения. Он подходит для теста, но Render может остановить его после 15 минут без входящего трафика, перезапустить сервис и ограничивает бесплатные часы в месяц. Для постоянно доступного туннеля в настройках Render можно выбрать любой платный тип сервиса; схема Docker и клиентская команда при этом не меняются.

После успешного деплоя Render выдаст адрес вида:

```text
https://wstunnel-proxy-xxxx.onrender.com
```

В клиенте используй тот же адрес с протоколом `wss://`:

```text
wss://wstunnel-proxy-xxxx.onrender.com
```

Внешний TLS завершается на Render, поэтому контейнер слушает внутренний `ws://0.0.0.0:${PORT}`. Не меняй `${PORT}` на фиксированный порт: Render передаёт его сервису автоматически.

## Android: запуск клиента

На Android нужен отдельный процесс wstunnel. Практичный вариант — **Termux из F-Droid**, а не случайная копия из стороннего сайта. Для большинства Android 14-телефонов с ARM64 скачай официальный asset `wstunnel_10.6.2_android_arm64.tar.gz` со [страницы релиза wstunnel](https://github.com/erebe/wstunnel/releases/tag/v10.6.2), распакуй бинарник и помести его в Termux как `~/bin/wstunnel`.

В Termux выполни:

```bash
pkg update
pkg install curl unzip net-tools
mkdir -p ~/bin
chmod +x ~/bin/wstunnel
```

Скопируй приложенный файл `wstunnel-phone.sh` в домашний каталог Termux и выполни:

```bash
chmod +x ~/wstunnel-phone.sh
~/wstunnel-phone.sh start
```

При первом запуске скрипт запросит секрет `WSTUNNEL_PATH` из Render, не показывая его на экране, и сохранит его только в `~/.wstunnel-render/path.secret` с закрытыми правами. Повторный запуск уже не потребует вводить секрет.

Для управления туннелем доступны команды:

```bash
~/wstunnel-phone.sh start
~/wstunnel-phone.sh status
~/wstunnel-phone.sh logs
~/wstunnel-phone.sh stop
~/wstunnel-phone.sh restart
```

После запуска скрипта SOCKS5-прокси доступен только на телефоне по адресу `127.0.0.1:10808`. Скрипт уже использует сервис `wss://swg-ocys.onrender.com`; менять адрес вручную не требуется.

Не отключай Termux от памяти и исключи его из оптимизации батареи Android, иначе система может остановить процесс. При перезапуске телефона команду нужно запустить заново, если не настроить отдельную автоматизацию запуска.

## Как пользоваться прокси

Для одного приложения, которое поддерживает SOCKS5, укажи:

| Параметр | Значение |
|---|---|
| Тип | SOCKS5 |
| Адрес | `127.0.0.1` |
| Порт | `10808` |
| DNS через прокси | включить, если приложение предоставляет такую настройку |

Например, в браузере с поддержкой SOCKS5 укажи эти параметры. Важно включить удалённое разрешение DNS, иначе DNS-запросы могут идти напрямую через мобильного оператора.

Для всего Android-трафика установи **SocksDroid** из [Google Play](https://play.google.com/store/apps/details?id=net.typeblog.socks) или используй релиз из [официального репозитория](https://github.com/bndeff/socksdroid). В приложении создай профиль с такими параметрами:

| Поле SocksDroid | Значение |
|---|---|
| SOCKS5 server / Host | `127.0.0.1` |
| Port | `10808` |
| Username / Password | оставить пустыми |
| UDP / Remote DNS | включить, если такие переключатели доступны |

Сначала в Termux выполни `~/wstunnel-phone.sh start`, затем открой SocksDroid и нажми кнопку запуска. Android покажет системное подтверждение VPN — разреши его. После этого значок VPN должен появиться в строке состояния, а трафик приложений будет идти через локальный SOCKS5 и Render. Если SocksDroid не подключается, проверь в Termux командой `~/wstunnel-phone.sh status`, что wstunnel работает.

## Проверка работы

После запуска клиента проверь, что локальный порт слушается:

```bash
pkg install net-tools
netstat -an | grep 10808
```

Затем проверь прокси из Termux:

```bash
pkg install curl
curl --socks5-hostname 127.0.0.1:10808 https://ifconfig.me
```

Если команда возвращает внешний IP, соединение через SOCKS5 работает. Если Render отвечает ошибкой 404/400 или клиент не подключается, сначала проверь точное совпадение `WSTUNNEL_PATH`, адрес `wss://` и наличие активного Render-сервиса.

## Ограничения

Этот вариант не использует WireGuard и не предоставляет стандартный VPN-профиль `.conf`. wstunnel передаёт динамические TCP-соединения через локальный SOCKS5, а SocksDroid добавляет системный Android VPN-интерфейс. Поддержка UDP зависит от связки и версии клиента; приложения с нестандартным сетевым поведением могут работать иначе.

Render Web Service может перезапускаться или менять инстанс, поэтому клиент должен автоматически переподключаться. Бесплатный инстанс также может уснуть после 15 минут без входящего трафика и имеет месячный лимит бесплатных часов; для постоянного использования нужен платный тип сервиса или готовность принять эти ограничения.

На iPhone этот сценарий существенно сложнее, поскольку нельзя просто запустить произвольный бинарник wstunnel в фоне. Для iOS потребуется приложение с нативной поддержкой нужного WebSocket-туннеля или промежуточный сервер/клиент, поэтому приведённые инструкции ориентированы на Android.

## Источники

1. [wstunnel README и CLI-документация](https://github.com/erebe/wstunnel)
2. [Render: Docker on Render](https://render.com/docs/docker)
3. [wstunnel v10.6.2 Android arm64 release](https://github.com/erebe/wstunnel/releases/tag/v10.6.2)
4. [Render: WebSockets](https://render.com/docs/websocket)
5. [wstunnel: обсуждение Android](https://github.com/erebe/wstunnel/issues/292)
6. [SocksDroid: официальный репозиторий](https://github.com/bndeff/socksdroid)
7. [SocksDroid в Google Play](https://play.google.com/store/apps/details?id=net.typeblog.socks)
8. [Render: бесплатные инстансы](https://render.com/docs/free)
